# A股 BB Mean Reversion 回测 — 第一轮红队审计答复

> 基于仓库代码 + 配置 + **实际回测数据**逐项回答。
> 每个问题均标注：文件 → 函数 → 关键代码 → 结论。
> 优先级：**最终回测版本 = `etf_live_backtest.py`**（产生 +226.75% 的唯一真实来源）。
> 无法仅凭代码确定处，明确标记【需要数据验证】。

---

## A. 交易时点 / 收盘后交易机制

### A1. 信号产生的完整时序
文件：`etf_live_backtest.py` → `run_backtest()`（L67-311）→ 每日循环（L89）

实际流程（按代码顺序）：
```
[T日盘中]  持仓处理 L123-191：
           止盈判断用 T日 high_adj (L129)   ← 盘中数据
[T日收盘后] 加仓判断用 T日 close_adj、bb_lower、is_limit_down (L165)
[T日收盘后] 空仓扫描：用 T日 amount 取Top10 (L200)、T日 close_adj 判下轨 (L203)
[T日收盘后] 以 T日 close 成交买入 (L209) / 加仓 (L170)
[T日收盘后] ETF再平衡 (L228)、估值入账 (L265-271)
[T+1日盘中] 首次可卖（hold_days≥1，L128/L129）
```
即：**T日盘中→止盈；T日收盘后→产生买入/加仓信号并成交**。没有"收盘后半小时窗口"的显式建模。

### A2. 成交价格 = T 日 close
文件：`etf_live_backtest.py` L209（空仓买入 `buy_price = candidate['close']`）、L170（加仓 `buy_price = r['close']`）。

结论：**使用 T 日 close 作为成交价**。不是 next_open，不是收盘后其他价格，是"信号价=成交价=当日收盘价"的抽象模型。
对应关系：真实 A 股中，以当日收盘价成交的唯一现实路径是**收盘集合竞价**（14:57-15:00，成交价=收盘价）。但收盘集合竞价申报时**不知道最终收盘价**，只能在竞价阶段申报"对手方最优价/市价"来确保成交——本模型简化为"收盘后直接以 close 成交"，属于**理想化成交假设（APPROXIMATION）**。

### A3. close 三重角色是否成立
close 同时用于：① BB 计算（close_adj=close×adj_factor）② 成交额排名依据（amount 与 close 同日）③ 买入成交价。

真实时序论证：
- **成立部分**：收盘后（15:00 后）amount、close、bb_lower 全部确定 → 用它们决策**无未来函数**（全部是 T 日收盘后已知信息）。
- **不成立部分**：决策时刻在 15:00 后，但成交价却是 15:00 刚产生的收盘价——现实中 15:00 之后无法再以 15:00 的价格成交（收盘集合竞价在 15:00 已结束）。要成交只能：① 提前在收盘集合竞价申报（此时不知最终价，存在滑点/不成交风险）；② 盘后固定价格交易（仅特定品种/投资者）。**模型未模拟这两条路径的成交不确定性**。
- 结论：**时序无未来函数 ✓，但成交可行性是乐观假设（APPROXIMATION，偏乐观方向）**。

### A4. Top10 排名确定时点
文件：`etf_live_backtest.py` L200 `top = pool.nlargest(top_n, 'amount')`。

amount 是**当日累计成交额**，收盘后才确定最终排名 → **T 日收盘后**才能确定 Top10。
策略下单（L207-222）发生在 L200 之后、同一交易日循环内 → **下单晚于排名确定时点** ✓ 无未来函数。

---

## B. 未来函数 / Look-ahead

### B1. 全项目扫描结果
对 `etf_live_backtest.py`、`live_backtest.py` 及全项目 `.py` 扫描 `shift(-1)` / `center=True` / `future` / `lead` / `forward` / `iloc[i+1]`：

- `etf_live_backtest.py`：`iloc[-1]` 仅出现在 **L317/L336（calc_stats 统计）**——计算总收益/年度收益，**不在交易循环内**。
- `live_backtest.py`：`iloc[-1]` 同样仅在 calc_stats（L240/L263）。
- **未发现** `shift(-1)`、`center=True`、`iloc[i+1]` 等。
- 结论：交易决策路径**无显式未来函数**。

### B2. BB(20,2) 数据窗口
文件：`etf_live_backtest.py` L50-53：
```python
g = df.groupby('ts_code')['close_adj']
df['ma20'] = g.transform(lambda x: x.rolling(20, min_periods=20).mean())
df['std20'] = g.transform(lambda x: x.rolling(20, min_periods=20).std())
df['bb_lower'] = df['ma20'] - 2 * df['std20']
```
`rolling(20)` 默认**左窗口（含当日，不含未来）**。T 日 bb_lower 由 T-19…T 构成 ✓ EXACT。

### B3. Top10 排名数据时点
L200 `nlargest(top_n, 'amount')`：amount 为当日列，`groupby('date')` 内排序 → 只使用 T 日当日数据 ✓ 无隐性未来信息。

### B4. daily_candidates 是否提前计算
`etf_live_backtest.py` **没有** daily_candidates 概念。数据预处理只做：合并 stock_basic、算 adj_close/high_adj、算 ma20/std20/bb（L38-55）——这些都是**按 ts_code 时序计算**（rolling 左窗口），无跨日候选缓存。候选选择完全在每日循环内实时完成（L193-223）✓。
> 注：仓库其他历史脚本（`strategy_v8_multi.py`、`full_market_v8.py`）曾预计算过候选，但**不是 +226.75% 的来源**。

---

## C. 复权因子（本轮重点）

### C1. adj_factor 口径
文件：`data/raw/adj_factor` 下载自 Tushare `adj_factor` 接口。

- Tushare `adj_factor` = **后复权因子**，含义：`后复权价 = 不复权价 × adj_factor`，用于把历史价格折算到**当前**口径。
- 当前使用 **`close_adj = close × adj_factor`（后复权）** 做指标，交易用 `close`（不复权）。
- **是"截至最新日期"的累计因子**——每只股票一个确定序列，历史日期因子值包含"该日之后"所有除权除息事件的累积。⚠️ 这是本策略最需要审计的点。

### C2. 是否 Point-in-Time 泄露 —— 数学证明
关键事实（**已用真实数据验证**，见 C5）：
**复权因子的定义保证 `adj_close = close × adj_factor` 在除权日连续**。
证明：
- 除权日 D：close 因除权从 P→P'（P'=P×除权比例），adj_factor 从 F→F'（F'=F/除权比例），故 `P×F = P'×F'`（adj_close 连续）。
- 非除权日：F 不变，adj_close 与 close 仅差常数倍。

对 BB 信号的影响：
- `signal_t = close_adj_t < (ma20−2σ)(close_adj)_t`，全部项都在**同一个连续序列** `close_adj` 上做 rolling 计算。
- 由于 `close_adj` 连续（除权日不跳变），**除权事件不制造假突破、假下穿** → 信号在除权日前后**不产生异常**（已实证）。
- "是否含未来分红"只改变 `close_adj` 的**绝对水平**（全体乘以一个单调累积因子），不改变其**相对形态与突破关系**——而 BB 是相对指标（当期值 vs 滚动均值±2σ）。
- **严格结论**：用"截至最新"的 adj_factor 计算 BB，**不会因为未来除权除息事件改变历史信号**（因为因子序列对每只股票是唯一的、且保证 adj_close 连续性；BB 对连续序列的 rolling 相对判断与因子绝对水平无关）。
- 唯一理论残留：某窗口内发生**多次大比例送转**时，`close_adj` 的波动率形态会被"除权跳变+连续化"轻微扭曲（窗口内因子变化大）。这是二阶效应，实证影响极小。

**评级：🟢 PASS（信号层面）**，残留风险仅为持仓跨除权的收益处理（见 C3）。

### C3. 止盈价转换 `bb_upper / adj_factor` 的正确性
文件：`etf_live_backtest.py` L130 `sell_price = r['bb_upper'] / r['adj_factor']`。

- bb_upper 是后复权上轨，除以**同一天的 adj_factor** 转回实际价——数学上自洽（同一序列的换算）。
- **已实证（C5）**：汾酒 2021-07-05 大比例送转（close 433→328、因子 4.425→6.197），除权日 adj_close 1919.92→2037.33（仅+6%正常波动），bb_upper 连续 → 转换后的实际止盈价水平正确。
- **残留问题（真实存在）**：回测**未处理持仓跨除权时的收益**：
  - 送转股：真实中股数增加、每股成本摊薄；回测**股数不变、avg_cost 不变**。
  - 现金分红：真实中应收现金；回测**不入账**。
  - 后果：持仓跨除权时，止盈目标（相对未摊薄 avg_cost）变远、且丢失分红/送转价值 → **收益被低估（保守方向）**。
  - **实证**：39 笔交易中 **3 笔**跨越复权跳变日（round22 山西汾酒 2023-05-23~07-25、round29 长安汽车 2024-05-06~07-10、round32 中兴通讯 2025-03-03~05-07），各自跨越的除权事件幅度仅约 2-4%，影响小。
- **评级：🟡 MINOR RISK（方向保守，量级小，3/39 笔）**。

### C4. "回测当时不知道未来分红"
严格解释：**是**——当前下载的 adj_factor 确实包含 2021-2026 的分红送转（例如 2020-01 的因子已含 2021 年 7 月汾酒的大送转）。
但（承 C2）：这**不改变 BB 信号**（连续序列+相对指标）。只改变指标的绝对水平，而止盈价转换是同序列自洽换算。
**对收益的影响路径**：不是通过信号，而是通过"跨除权持仓的收益处理缺失"（C3）——且方向保守。
结论：存在"未来分红已知"这一事实，但**未构成对历史信号的未来函数**；需标注为已知近似。

### C5. 逐日验证（已执行）
取山西汾酒 600809.SH（7 次复权跳变）等验证（详细数据见附录 A）：
```
2021-07-05 除权: close 433.88→328.76 | adj_factor 4.425→6.197 | adj_close 1919.92→2037.33 (+6%, 正常波动)
2022-06-28 除权: adj_close 1979.94→1979.31 (几乎连续)
```
- 除权日 **adj_close 全部连续**（无跳变假象）。
- **BB 信号在除权日前后无异常**（汾酒 2021-06-20~07-15 窗口内信号仅在 06-23 出现 1 次，系正常跌破，非除权假信号）。
- 取数、算法与回测完全一致（close×adj_factor、rolling20 左窗口）。

---

## D. 跌停判定

### D1. 9.5% 阈值确认
是。数据列 `is_limit_down` 在**数据构建脚本**生成：`strategy_optimized.py` L86 / `full_market_v8.py` L59 / `strategy_v8_multi.py` L98：
```python
merged['is_limit_down'] = merged['close'] <= merged['pre_close'] * 0.905
```
（`combined_daily.parquet` 的 `is_limit_down` 列即由此生成，已在数据中。）

### D2. 股票池市场构成与涨跌停
- 数据构建时 `download_data.py` L96-98：`~ts_code.str.startswith(('8','4','9'))` → **排除了北交所**。
- 池内包含：主板（60/00）、创业板（30）、科创板（688）。
- 涨跌停判定：**统一用 0.905 固定阈值**，未按板块区分（创业板/科创板应为 ±20%）。
- **已核实：股票池含创业板、科创板，但其涨跌停按 9.5% 判——不匹配。**

### D3. 误判类型（具体举例）
| 板块 | 真实涨跌幅 | 0.905 阈值行为 |
|---|---|---|
| 主板 | ±10% | 收盘跌 9.5%~9.99% 误判为跌停（**误禁买**） |
| 创业板/科创板 | ±20% | 跌 9.5%~19.99% **全部误判为跌停**（严重误禁买） |
| ST 主板 | ±5%（2026.7前） | 跌 5% 收盘**不被识别**（漏判） |
| 北交所 | ±30% | 已排除出池（不适用） |

**影响方向：偏保守**（误判为跌停 → 该买没买）。但因该策略只买"收盘<下轨"的超跌票，而这些票常真跌停，误判率需数据核验。【需要数据验证：统计 is_limit_down 中"20%板股票 9.5~20%跌幅"的占比】

### D4. 跌停判断的使用范围
调用链：`is_limit_down` 列 → 仅用于**买入/加仓过滤**：
- L165（加仓）：`not r['is_limit_down']`
- L203（空仓买入）：`not r['is_limit_down']`
**不影响卖出**（卖出只看 bb_upper / time_stop）。当前版本无时间止损，实际卖出只有上轨止盈。

### D5. 是否允许跌停价卖出 —— 关键确认
文件：`etf_live_backtest.py` L129-146。卖出触发条件 `high_adj >= bb_upper`（涨到上轨）。
- **一字跌停日 high=low=跌停价，不可能 ≥ bb_upper** → 止盈逻辑在跌停日**不会触发卖出**（实际安全）。
- 但若"涨停次日开盘点位在上轨附近"等非极端情形，模型按理论价成交，未检查当日是否无法卖出。
- 当前版本（无时间止损）下，**"跌停日被强制卖出"的实际发生概率极低**。评级：🟡 MINOR（模型假设，无实际触发案例，需数据复核）。

---

## E. T+1

### E1. 控制变量与代码
文件：`etf_live_backtest.py`
- L77-78：`pos = None`（持仓字典）
- L221：`'entry_day_idx': i`（入场日索引）
- L128：`hold_days = i - pos['entry_day_idx']`
- L129：`hold_days >= 1` 才允许止盈卖出
**证明**：T 日买入记 `entry_day_idx=i`，当日及当日循环内 `hold_days=0` → 不可能进 L129 卖出分支。T+1 日 `hold_days≥1` → 可卖。✓ **EXACT**

### E2. 买入→加仓→止盈同日？
- 加仓（L165）在**持仓处理块**，发生在空仓买入（L193）**之前**。
- 买入当日：进入 L193 时 pos 刚建立，**不会再回到 L165**（L193 在 L165 之后）。
- 止盈需 `hold_days≥1`，买入日 hold_days=0 → 不可能同日止盈。
**结论：不可能同日卖出。** ✓ EXACT

### E3. 多次加仓后 T+1 是统一还是分批
实现：`entry_day_idx` 是**整个持仓轮次**的入场日（L221 只在首次买入时设置），`hold_days` 基于**首次入场日**计算。
- 即：**整个持仓统一 T+1**（从首次买入日起算），加仓股与首仓同一天可卖。
- 真实 A 股：加仓股本身也需 T+1（加仓日次日起可卖）。本模型**允许加仓股与首仓同日卖出**——即加仓股可能持有不足 1 天就被卖。
- 影响：偏乐观（放宽了 T+1），但加仓后次日才可能触发止盈（止盈是盘中条件，加仓在收盘后，最早止盈是 T+1 盘中），实际加仓股至少持有到次日收盘前。风险小。**评级：🟡 MINOR（实现是"轮次级 T+1"，严格应为"批次级 T+1"）**。

---

## F. 加权平均成本

### F1. 数学公式
文件：`etf_live_backtest.py` L176-178：
```python
old_cost = pos['shares'] * pos['avg_cost']
pos['shares'] += qty
pos['avg_cost'] = (old_cost + cost_add) / pos['shares']
pos['total_cost'] += cost_add
```
其中 `cost_add = amount + calc_fee_buy(amount)`（L174-175）。
公式：`avg_cost_new = (shares_old×avg_cost_old + price×qty + 买入费) / shares_new`。
**旧持仓成本 + 新买入资金 + 买入手续费 全部纳入** ✓ EXACT。

### F2. 加仓后卖出 PnL
L131-135（止盈）：`amount = sell_price × shares`；`fee = calc_fee_sell(amount)`；`proceeds = amount − fee`；`pnl = proceeds − pos['total_cost']`。
`total_cost` 累加全部买入（含费），`proceeds` 为卖出净收入（扣费）→ **总买入成本 − 总卖出净收入** ✓ EXACT。

### F3. 手续费是否重复扣除 —— 两笔加仓验证
案例：买1000股@20（费5.2）→ 加1000股@18（费5.18）→ 卖2000股@19.5（费29.64）：
- total_cost = 20000+5.2 + 18000+5.18 = 38010.38
- proceeds = 39000 − 29.64 = 38970.36
- pnl = 38970.36 − 38010.38 = **959.98**（= 毛利 1000 − 费用合计 40.02）✓
**买入费只在 total_cost 计入一次，卖出费只在 proceeds 扣除一次，无重复** ✓ EXACT。
（注：avg_cost 含买入费使止盈目标略高，属保守；pnl 用净额，无重复。）

### F4. 卖出费用扣除
L29-30 `calc_fee_sell`：`max(amount×0.00025,5) + amount×0.0005(印花税) + amount×0.00001(过户费)` → 在 `proceeds = amount − fee`（L133）中一次扣除 ✓ EXACT。

---

## G. 仓位与现金

### G1. 目标仓位基准
文件：L67-69 `level_cash=200_000`（固定，=初始资金 100 万的 20%）；L168/L208 `ensure_cash_needed(level_cash)`；L171/L210 `qty = int(min(level_cash, cash) / price / 100) × 100`。
**使用固定 `level_cash`（初始资金的20%），不是当前 total_equity 的20%。**

### G2. 新增 20% 的基准
**初始资金的 20%**（固定 20 万元/层），不是当前总资产、也不是当前现金。当账户因盈利变大时，每层仍是 20 万（相对总资产比例递减）。

### G3. 现金不足仍成交？
- 空仓买入：L215 `if cost_add <= cash`（严格检查）✓
- 加仓：L172 `if qty >= 100 and buy_price*qty + calc_fee_buy(...) <= cash`（严格检查）✓
- 买入前均调用 `ensure_cash_needed`（L98-119）卖 ETF 补足。
- **不存在"现金不足仍成交"** ✓ EXACT。
- 注：`cash >= -fee.net_cash_flow` 是 VectorBT 时代 `backtest/vbt_runner.py` 的写法，**最终引擎未使用**。

### G4. 浮盈是否扩大加仓金额
**不会**：加仓金额被 `min(level_cash, cash)` 锁死为 ≤20 万/层（L171）。浮盈只体现为 cash 增加（若 ETF 卖出或止盈），但每层加仓仍 ≤20 万。**无仓位漂移，属策略设计（固定金额分层）** ✓。

---

## H. 滑点与手续费

### H1. +226.75% 是否扣滑点
文件：`etf_live_backtest.py` **全文无 `slippage`/`slip` 代码**。费用仅 calc_fee_buy/sell/etf（L25-35）。
`config.yaml` 中有 slippage 配置，但**最终引擎未读取**。
**结论：+226.75% 未扣除任何滑点。** 这是本结果最显著的乐观偏差之一。**评级：🟠 MATERIAL RISK（方向乐观）**。

### H2. 费率是否固定覆盖 2020-2026
是，全部固定（L19-22）：
- 佣金 0.00025（万2.5，最低5元）
- 印花税 0.0005（万5，仅卖出）
- 过户费 0.00001（万0.1，双向）
- **未随时间动态调整**。

### H3. 印花税历史变化是否模拟
**否**。真实规则：
- 2023-08-28 前：印花税 **0.001（万10）**（2008-09 ~ 2023-08）
- 2023-08-28 起：减半至 **0.0005（万5）**
代码全程用 0.0005 → **2020-01~2023-08-27 的卖出印花税被低估一半（0.05%/笔卖出金额）**。
影响：系统性、乐观方向。估算：约 39 笔轮次 × 每笔卖出 20-200 万 × 0.05% ≈ 数千元~数万元，相对 +226 万收益占比小但为**方向偏差**。**评级：🟡 MINOR~🟠（方向乐观，量级可控）**。

---

## I. 幸存者偏差

### I1. 缺失退市股（已用数据验证）
- `stock_basic`：339 只退市（D）。回测数据含 **214 只（63%）**，缺 **125 只**。
- 其中 **15 只 2020 年后退市**（应出现在回测区间）：乐视退、康得退、凯迪退、龙力退、金亚退、千山退、退保千、退华业、退金泰、德奥退、*ST金洲、神城A退、观典防务、泰祥股份、翰博高新。
- 其余 110 只为 2019 年前退市（不在回测区间，无影响）。

### I2. 缺失股是否进过 Top10
- **在库退市股**（可查）：214 只中 **7 只**曾进入当日成交额 Top10（14 条股票-日期），但**全部不满足 BB 下轨信号（0 条）** → 即使在场也不会被买入。
- **缺失的 15 只**：无数据，**无法直接验证**【需要数据验证】。其特征（退市前长期停牌+连续跌停+ST）决定了大概率被"ST 排除 + is_limit_down 过滤"拦截。

### I3. 曾进 Top10 的退市股是否触发过交易
在库 7 只进过 Top10 的退市股，**0 条 BB 信号** → 无理论买入 → 无理论卖出。实证：**在库部分对结果零影响**。

### I4. 数据方最小输出（如需验证缺失 15 只）
对缺失的 15 只退市股（乐视退、康得退等），输出最小 CSV：
```
ts_code, trade_date, open, high, low, close, pre_close, vol, amount, adj_factor
```
（2020-01-01 ~ 各股退市日；`adj_factor` 用退市时刻的因子即可）。拿到后可复跑"是否进 Top10 + BB 信号"判定。

**评级：🟡 MINOR（在库部分已实证零影响；缺失 15 只方向预计保守，待数据最终确认）**。

---

## J. 停牌 / 涨停 / 跌停 / 成交可行性

### J1. 停牌期间是否交易
文件：L187-189。持仓股当日无行情 → 用 `last_close` 估值，**无交易**。停牌日不在候选池（无当日行）。✓ 停牌不交易。

### J2. 一字涨停是否可能被买入
买入条件 `close_adj < bb_lower`（L203/L165）——**收盘跌破下轨**。一字涨停是收盘大涨，**不可能同时满足收盘<下轨** → **逻辑上不会买入涨停股** ✓ 天然安全（非显式检查，但条件互斥）。

### J3. 一字跌停是否可能被卖出
- 止盈（L129）：需 `high_adj ≥ bb_upper`，一字跌停 high=跌停价，不可能 ≥ 上轨 → **不会触发** ✓。
- 时间止损（L147-164）：用 close 卖，**若启用且当日一字跌停，模型仍按 close 成交（能卖）**——但当前版本 `time_stop_days=None` **未启用**。
- 结论：当前版本实际卖出路径（上轨止盈）不受一字跌停影响 ✓；时间止损启用时存在此风险（未启用）。

### J4. 盘中可成交性判断
日线只有 OHLC，**模型无法判断盘中是否实际可成交**（如涨停封板买不进、跌停封板卖不出）。当前实现**未做任何成交可行性检查**（仅靠"收盘<下轨与涨停互斥"、"止盈需>上轨与跌停互斥"两个间接约束）。**这是明确标注的模型假设（UNSUPPORTED）**。

---

## K. 策略版本一致性

### K1. 五者一致性
| 文件 | 描述版本 | 一致性 |
|---|---|---|
| `etf_live_backtest.py` | Top10 + 上轨止盈 + 5层 + ETF现金管理 | ✅ 真实结果来源 |
| `AUDIT_GUIDE.md` | 同上（最终版） | ✅ 一致 |
| `README.md` | 同上 | ✅ 一致 |
| `CALLCHAIN.md` | 同上 | ✅ 一致 |
| `config/config.yaml` | **Top1 + 1.5%止盈**（原始设计） | ⚠️ **落后于实现**，是历史配置，非最终策略 |
| `results/etf_Top10_5层_ETF现金管理.parquet` | 最终结果 | ✅ 与 etf_live_backtest.py 一致 |

### K2. +226.75% 唯一来源
`etf_live_backtest.py`（`main` L356-378 中 `top_n=10, max_levels=5, etf_enabled=True, etf_ratio=1.0` 配置）。
其他文件（`strategy_optimized.py`、`strategy_improved.py`、`strategy_v8_multi.py`、`full_market_v8.py`、`live_backtest.py` 等）为历史/变体版本，**均不是该数字的来源**。

### K3. 复现命令
```bash
cd /Users/mouha/DoubaoWork/chats/2026-08-25/new-chat
python3 etf_live_backtest.py
```
所需数据：`data/combined_daily.parquet`（Tushare daily+adj_factor 合并）、`data/etf_513500_merged.parquet`（标普500ETF）、`data/raw/stock_basic.parquet`。全部本地化，无需联网/token。输出：`results/etf_Top10_5层_ETF现金管理*.parquet/csv`。

---

## L. 审计结论（逐项评级）

| # | 项目 | 评级 | 说明 |
|---|---|---|---|
| 1 | 未来函数 | 🟢 PASS | 决策路径无 shift(-1)/center；`iloc[-1]` 仅在统计 |
| 2 | 收盘后交易时序 | 🟠 MATERIAL RISK | 用 T 日 close 成交是理想化假设，未模拟收盘集合竞价申报的不确定性（乐观） |
| 3 | Top10 信息时点 | 🟢 PASS | 收盘后确定排名，下单晚于信息确定 |
| 4 | 复权因子 | 🟡 MINOR | BB 信号对除权不变性已实证；残留=跨除权持仓收益未入账（3/39笔，保守） |
| 5 | 跌停判定 | 🟡 MINOR | 固定9.5%阈值未分板块，创业板/科创板被过度禁买（保守） |
| 6 | T+1 | 🟡 MINOR | 主路径严格；加仓为"轮次级T+1"非"批次级"（轻微乐观） |
| 7 | 加权成本 | 🟢 PASS | 含费加权，无重复扣除（两笔案例验证） |
| 8 | 手续费 | 🟢 PASS | 结构正确；费率固定（见9） |
| 9 | 滑点 | 🟠 MATERIAL RISK | **完全未扣滑点**（+226.75% 的最显著乐观偏差） |
| 10 | 幸存者偏差 | 🟡 MINOR | 在库7只退市股进Top10但0信号（实证零影响）；缺失15只待数据确认 |
| 11 | 停牌 | 🟢 PASS | 停牌日不交易、用 last_close 估值 |
| 12 | 涨停成交 | 🟢 PASS | 买入条件与涨停互斥，逻辑上不买涨停股 |
| 13 | 跌停成交 | 🟡 MINOR | 止盈不会在跌停日触发；时间止损未启用（启用后有风险） |
| 14 | 仓位 | 🟢 PASS | 固定20万/层，无漂移，无超买 |
| 15 | 版本一致性 | 🟡 MINOR | 结果来源唯一且可复现；config.yaml 为陈旧配置需注意 |

**核心问题回答：修复所有 MATERIAL/INVALID 项后，+226.75% 是否仍可信？**

结论：**大概率仍具统计可信度，但最终数字会下修。**

理由：
1. **两个 MATERIAL 项的方向**：无滑点（乐观）+ 收盘价成交假设（乐观）。修复=加入滑点（约万1-万3）+ 改用 next_open 或收盘竞价近似 → 收益预计从 +226.75% 下修，但**策略的 82.1% 胜率、39 笔样本、相对基准（纳斯达克+207.7%）的超额**是结构性特征，滑点/成交假设难以完全抹平。
2. **主要偏差方向多为保守**（复权跨除权低估、跌停误禁买、幸存者偏差有限），与两个乐观项部分对冲。
3. **样本量是更大隐患**：39 笔交易偏少，统计置信区间较宽——即使数字成立，Sharpe 1.16 的置信度有限。**建议**：修复后重跑 + 扩大样本（放宽 top_n / 增加时间段）验证稳健性。
4. 结论表述：**+226.75% 是一个"上界"性质的估计**；修复乐观偏差后，合理预期区间约 +130% ~ +190%（量级仍跑赢标普500 +132%），但需重跑确认。

---

## 附录 A：C5 复权连续性验证数据（山西汾酒 600809.SH）

```
2021-07-05 除权: close 433.88→328.76 | adj_factor 4.425→6.197 | adj_close 1919.92→2037.33 (+6%正常波动)
2022-06-28 除权: close 319.50→317.60 | adj_factor 6.197→6.2321 | adj_close 1979.94→1979.31 (几乎连续)
2023-06-26 除权: close 203.60→195.60 | adj_factor 6.233→6.336 | adj_close 1269.04→1239.32 (-2.3%)
2024-07-05 除权: close 201.35→193.50 | adj_factor 6.336→6.4766 | adj_close 1275.75→1253.22 (-1.8%)
2025-01-24 除权: close 177.08→173.76 | adj_factor 6.4766→6.5678 | adj_close 1146.88→1141.22 (-0.5%)
```
BB 信号在除权日前后无异常跳变（窗口内唯一信号 2021-06-23 为真实跌破）。

## 附录 B：在库退市股 Top10 核验（I 节实证）

- 214 只在库退市股中 7 只曾进入当日成交额 Top10（14 条），**全部不满足 BB 下轨信号（0 条）** → 对回测结果零影响。
- 缺失 15 只（2020 后退市）需数据方输出最小 CSV（见 I4）最终确认。
